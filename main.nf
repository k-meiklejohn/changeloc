include RUN_TPPRED3 from 
include  from 
include  from 
include  from 
include  from 

workflow {
    input_ch = channel.fromPath("example.fasta")

    RUN_TPPRED3(input_ch)
}

workflow {
    input_ch = channel.fromPath("example.fasta")

    RUN_TPPRED3(input_ch)
}

workflow {
    input_ch = channel.fromPath("example.fasta")

    RUN_TPPRED3(input_ch)
}

workflow {
    input_ch = channel.fromPath("example.fasta")

    RUN_TPPRED3(input_ch)
}

workflow {
    input_ch = channel.fromPath("example.fasta")

    RUN_TPPRED3(input_ch)
}